# 문제1
exam1 <- function(){
  a <- LETTERS[1:26]
  b <- letters[1:26]
  return(paste(a,b,sep=""))
}
exam1()


# 문제2
exam2 <- function(x) {
  sum <- 0
  while(sum <= 0)
  sum2 <- sum + sum + 1;
  cat(sum2, "\n")
  if(sum2 == (x-1))
    break;
  }

exam2(3)


while(sumNumber <= 20) {
  i <- sample(1:5,1)
  sumNumber <- sumNumber+i;
  cat(sumNumber, "\n")

sum = 0
for(i in 5:15) {
  if(i%%10==0) {
    break;
  }
  sum <- sum + i
  print(paste(i,":",sum))
}


sumNumber <- 0
repeat {
  i <- sample(1:5,1)
  sumNumber <- sumNumber+i;
  cat(sumNumber, "\n")
  if(sumNumber > 20)
    break;
}

sumNumber <- 0
while(sumNumber <= 20) {
  i <- sample(1:5,1)
  sumNumber <- sumNumber+i;
  cat(sumNumber, "\n")
}


# 문제3
exam3 <- function(x,y) {
  return(max(x,y)-min(x,y))
}
exam3(10,20)

# 문제4
exam4 <- function(x,y,z) {
  is.numeric(x)
  is.
}

  





