#문제1

mySum <- function(vec){
  if(sum(vec%%2==0,vec%%2==1))
     return(list(evenSum,oddSum))
}if(is.vector(vec)),
(!is.vector(vec))
stop("벡터만 처리 가능!!")

[vec(min(vec),na.rm=T)
   warning("NA를 최솟값으로 변경하여 처리함!!")
   vec(min(vec),na.rm=T)
  if((vec)<-NULL)
    
   return()
   

  
  #문제2
myExpr <- function(ft){
  if(is.function(ft)
     stop("수행 안할꺼임!!"))
    sample(1:45, 6)
}
return(result)


#문제3
createVector<- function(...){
  if(any(is.na(...)){
    return("NA")
  }
else(all(!is.na(...)){
    return()
}
       return())
}
result<-
return(...)




#문제4
data<-read.table("C:/Rstudy/iotest1.txt")
arrange(data)
arrange(desc(data))
sum(data)
mean(data)



#문제5
data<-read.table("C:/Rstudy/iotest2.txt")
for(count(data) in data)
cat("가장 많이 등장한 단어는", data, "입니다.")
