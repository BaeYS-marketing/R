getwd()
setwd("c:\R_Statistics\Part3\\")
setwd("c:/R_Statistics/Part3")
setwd("C:/R_Statistics/Part3")
setwd("C:/R_statistics/Part3")
data<- read.csv('one_sample.csv')
data
View(data)
head(data,6)
head(data,6)
summary(data)
summary(x)
table(x)
x<- data$survey
str(x)
summary(x)
table(x)
#결측치 확인
summary(x)  'summary는 만능- 수치형이면 수치, 팩터면 팩터로'
#결측치 확인
summary(x)  #'summary는 만능- 수치형이면 수치, 팩터면 팩터로'
table(x)    #'talbe은 팩터형을 받는다. 아니면 자동변환'
summary(x)  #'summary는 만능- 수치형이면 수치, 팩터면 팩터로'
table(x)    #'talbe은 팩터형을 받는다. 아니면 자동변환'
f<- as.factor(x)
summary(f)
#install.packages("prettyR")
library("prettyR")
freq(x)
#앞에 있는 아규먼트를 기준으로 80%다 라는 양측검정이 시작된다.
binom.test(c(136,14),p=0.8)
freq(x)
getwd()
data <- read.csv("one_sample.csv", header=T)
head(data)
computer<-data$time
#전반적인 확인  _ 결측치 확인
summary(computer)
mean(computer,na.rm=T)
'양측검정 HO : 만족비율이 80%이다.
대립가설 만족비율이 80%가 아니다.'
#앞에 있는 아규먼트를 기준으로 80%다 라는 양측검정이 시작된다.
#비율검정 : 한집단 binom , 두집단 이상 prop
binom.test(c(136,14),p=0.8)
#전반적인 확인  _ 결측치 확인
summary(computer)
#전반적인 확인  _ 결측치 확인
summary(computer)
mean(computer,na.rm=T)
#전반적인 확인  _ 결측치 확인
summary(computer)
head(data)
#결측치 처리
table(survey)
#결측치 처리
table(computer$survey)
#결측치 처리
table(computer$survey)
#결측치 처리
?table
table(computer$survey)
computer$survey
computer$survey
computer$survey
computer$survey
computer$survey
getwd()
data <- read.csv("one_sample.csv", header=T)
head(data)
computer<-data$time
#전반적인 확인  _ 결측치 확인
summary(computer)
mean(computer,na.rm=T)
#수치 : 더 정확하게 파악할 수 있다.
shapiro.test(com)
com<- na.omit(computer)
summary(com)
#수치 : 더 정확하게 파악할 수 있다.
shapiro.test(com)
#시각화
hist(com)
#시각화
hist(com)
qqnorm(com)
#t검정
t.test(com, mu=5.2)
#앞에 있는 아규먼트를 기준으로 80%다 라는 양측검정이 시작된다.
#비율검정 : 한집단 binom , 두집단 이상 prop
binom.test(c(136,14),p=0.8)
